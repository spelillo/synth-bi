// api/_supabaseAuth.js — ported from synth-sql as-is: verifies the bearer
// token on incoming requests and resolves it to a Supabase user id. Shared
// by api/chat.js and any future signed-in-only export endpoint.
//
// STUB — port getVerifiedUserId().
export async function getVerifiedUserId(_req) {}
