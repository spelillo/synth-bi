// dashboard/src/Dashboard.jsx — the dashboard canvas: tile grid (drag/resize
// via react-grid-layout), "+ Add tile" entry point into TileEditor, the
// "Preview as: Synth / Power BI / Tableau" toggle (initial-build.md §8a),
// and the AI panel (AiPanel.jsx) alongside it. Reads/writes tiles through
// ./bridge.js — never owns tile state independently of the shell.
//
// STUB — see BUILD-INSTRUCTIONS.md for the Dashboard tab's full layout spec.

export default function Dashboard() {
  return null;
}
